nand2[a b] -> out
{
    out <- !(a & b);
}

nand3[a b c] -> out
{
    out <- !(a & (b & c));
}

jk[clk j k] -> q qbar
{
    q <- nand2[nand3[clk k q] qbar];
    qbar <- nand2[nand3[clk j qbar] q];
}

main[clk_last j k] -> clk q qbar
{
    q qbar <- jk[clk_last j k];
    clk <- !clk_last;
}
